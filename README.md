# API Practice - 教材用最小リポジトリ

[![CI Pipeline](https://github.com/gurupage/ApiItPractice/actions/workflows/ci.yml/badge.svg)](https://github.com/gurupage/ApiItPractice/actions/workflows/ci.yml)

Clean Architecture + Spring Boot + JUnit5 + Testcontainers（Oracle XE）の教材リポジトリ

## 🏗️ アーキテクチャ

```
┌─────────────┐
│  Interface  │  ← REST API（Controller, DTO）
└──────┬──────┘
       │
┌──────▼──────┐
│   Usecase   │  ← ビジネスロジック（Service, Port）
└──────┬──────┘
       │
┌──────▼──────┐
│    Core     │  ← ドメインモデル（Task, TaskStatus）
└─────────────┘
       ▲
       │
┌──────┴──────────┐
│ Infrastructure  │  ← Adapter（Repository, HTTP Client）
└─────────────────┘
```

## 📁 ディレクトリ構成

```
src/
├── main/java/com/example/apipractice/
│   ├── core/domain/              # ドメインモデル（依存なし）
│   ├── usecase/                  # ビジネスロジック
│   │   ├── port/                 # ポート（インターフェース）
│   │   └── service/              # サービス
│   ├── infrastructure/           # アダプタ
│   │   ├── persistence/          # DB実装
│   │   └── client/               # 外部API実装
│   └── interfaces/rest/          # REST API
└── test/java/com/example/apipractice/
    ├── unit/                     # UT（Spring起動なし）
    │   ├── usecase/
    │   └── interfaces/
    └── integration/              # IT（Spring起動あり）
        ├── config/
        ├── infrastructure/
        └── api/
```

## 🚀 セットアップ

### 前提条件

- JDK 21
- Docker Desktop（Testcontainers用）
- IntelliJ IDEA（推奨）

### 1. リポジトリクローン

```bash
git clone <repository-url>
cd api-practice
```

### 2. Gradle依存関係の取得

```bash
./gradlew build --no-daemon
```

### 3. ローカルDB起動（オプション）

```bash
docker-compose up -d
```

## 🧪 テスト実行

### UT実行（高速、Mockのみ）

```bash
./gradlew unitTest
```

**実行時間**: 約1〜2秒
**実行内容**: ビジネスロジックの分岐、例外ハンドリング

**詳細ドキュメント**:
- [単体テスト作成ガイドライン](./docs/unit-test-guidelines.md) - テスト名の付け方、AAAパターン、ベストプラクティス
- [単体テストサマリー](./docs/unit-test-summary.md) - テストケース一覧、カバレッジ目標

### 実行時間計測スクリプト

UT/ITの実行時間を計測してサマリーを表示:

```bash
# Linux/Mac
./scripts/measure-test-time.sh [ut|it|all]

# Windows
scripts\measure-test-time.bat [ut|it|all]
```

**オプション**:
- `ut` または `unit`: UTのみ実行
- `it` または `integration`: ITのみ実行
- `all`: すべてのテスト実行（デフォルト）

**出力例**:
```
╔════════════════════════════════════════════════════╗
║          📊 Test Execution Summary                 ║
╠════════════════════════════════════════════════════╣
║ 🧪 Unit Tests         │     5s (00:05)      ║
║ 🔬 Integration Tests  │   263s (04:23)      ║
╠════════════════════════════════════════════════════╣
║ 📦 Total              │   268s (04:28)      ║
╚════════════════════════════════════════════════════╝
```

### IT実行（Testcontainers + WireMock）

```bash
./gradlew integrationTest
```

**実行時間**: 約3-6分（改善前の構成 - テストクラスごとにコンテナ再起動）
**実行内容**: E2Eフロー、DB永続化、外部API連携、Flywayマイグレーション

**HTTPテストツール**: TestRestTemplate（実HTTPリクエスト、教材として最適）

**注意**: 現在の構成は意図的に非効率（教材用）です。
- 各テストクラスでOracleコンテナを再起動
- 改善により70-80%の時間削減が可能

**詳細ドキュメント**:
- [統合テスト作成ガイドライン](./docs/integration-test-guidelines.md) - TestRestTemplate選定理由、AAAパターン
- [統合テストサマリー](./docs/integration-test-summary.md) - テストケース一覧、ツール比較
- [統合テスト実行ガイド](./docs/how-to-run-integration-tests.md) - 実行方法、トラブルシューティング
- [WireMock使用ガイド](./docs/wiremock-guide.md) - 外部APIモック、テスト内起動、スタブ設定

### 全テスト実行 + JaCoCoレポート

```bash
./gradlew ci
```

**レポート確認**:
```bash
open build/reports/jacoco/test/html/index.html
```

## 🗄️ Flyway + Oracle Testcontainers

### Oracleイメージ: `gvenzl/oracle-xe:21-slim`

**選定理由:**
- 軽量（約2.5GB vs 公式の5GB+）
- 高速起動（30-60秒 vs 公式の2-5分）
- Testcontainers公式サポート
- Oracle 21c XE（本番互換性が高い）

**マイグレーションファイル:**
- `src/main/resources/db/migration/V1__create_task_table.sql`
- Oracle固有の構文（`NUMBER`, `VARCHAR2`, `CLOB`, `IDENTITY列`）

**詳細ドキュメント:**
- [Oracle Testcontainersセットアップ](./docs/oracle-testcontainers-setup.md)
- [統合テスト実行ガイド](./docs/how-to-run-integration-tests.md)

### 現在の構成（改善前）

```java
@Container
static OracleContainer oracleContainer =
    new OracleContainer("gvenzl/oracle-xe:21-slim")
        .withReuse(false);  // ← わざと再利用しない（教材用）
```

**問題点:**
- テストクラスごとにコンテナ再起動（3-6分）
- メモリ使用量が大きい

**次のステップ:**
- コンテナ共有化で70-80%高速化
- Spring Context再利用

## 📊 JaCoCo カバレッジ

- **目標**: 70%以上
- **除外対象**: DTO, Configuration, Application.class

## 🎯 API仕様

### 1. タスク作成

```bash
POST /tasks
Content-Type: application/json

{
  "title": "サンプルタスク",
  "description": "説明文"
}
```

**レスポンス**: 201 Created

### 2. タスク取得

```bash
GET /tasks/{id}
```

**レスポンス**: 200 OK

### 3. タスク完了

```bash
POST /tasks/{id}/complete
```

**レスポンス**: 200 OK（status: DONE）

## 🔍 UT/ITの境界

| テストタイプ | Spring起動 | DB | 外部API | 対象 |
|-------------|-----------|----|---------|----- |
| **UT** | ❌ | Mock | Mock | ロジック分岐 |
| **IT** | ✅ | Testcontainers | WireMock | E2E動作 |

## 🛠️ 技術スタック

- Java 21
- Spring Boot 3.2.2
- JUnit 5
- Mockito
- Testcontainers（Oracle XE 21c）
- **WireMock**（外部HTTP APIモック）
- JaCoCo
- Flyway

### 外部API依存（WireMockでモック）

| API | Port | 用途 |
|-----|------|------|
| **通知API** | 8081 | タスク作成通知 |
| **ユーザー検証API** | 8082 | ユーザー存在確認 |

## 📖 学習ポイント

### 1. Clean Architectureの実装
- Core → Usecase → Infrastructure の依存方向
- ポート（インターフェース）による DIP
- 外部依存の抽象化（Port/Adapter）

### 2. テスト戦略
- **UT**: 高速、Mockito、ロジック検証
  - 外部依存は全てモック化
  - ビジネスロジックの分岐を網羅
- **IT**: 実環境、Testcontainers、E2E検証
  - 実HTTPリクエスト（TestRestTemplate）
  - Oracle Database（Testcontainers）
  - 外部API（WireMock）

### 3. 外部依存の扱い方
- **Port/Adapter分離**: 外部APIをインターフェースで抽象化
- **WireMockでモック**: テスト時に外部APIを置き換え
- **設定の外部化**: `@Value`でURL設定

### 4. CI/CD（GitHub Actions）
- **自動テスト**: UT + IT自動実行
- **時間計測**: UT/IT個別の実行時間を可視化
  - Gradleビルドスキャン不要
  - ログから所要時間を確認可能
- **JaCoCoレポート**: 自動生成・アップロード
- **Oracle Testcontainers対応**:
  - Docker環境の確認
  - タイムアウト設定（IT: 20分）
  - メモリ管理（ubuntu-latest: 7GB）
- **アーティファクト**: テスト結果・カバレッジレポート（30日保持）

詳細: [CI/CDガイド](./docs/ci-cd-guide.md)

## 🔧 トラブルシューティング

### Docker Desktopが起動していない

```
Caused by: Could not find a valid Docker environment
```

→ Docker Desktopを起動してください

### Oracle XEイメージのダウンロードが遅い

初回実行時は約1GB のダウンロードが発生します（数分）

### ポート競合

```
Address already in use: bind
```

→ `docker-compose down` で既存コンテナを停止してください

## 🚀 CI/CD

### GitHub Actions

プッシュ・PR作成時に自動実行:

1. ✅ UT実行（< 5秒）
2. ✅ IT実行（3-6分、Oracle Testcontainers）
3. ✅ JaCoCoレポート生成
4. ✅ アーティファクトアップロード（テスト結果・カバレッジ）
5. ✅ 実行時間サマリー表示

**詳細**: [CI/CDガイド](./docs/ci-cd-guide.md)

### ローカルでCI環境を再現

```bash
# 環境変数設定
export GRADLE_OPTS="-Dorg.gradle.daemon=false -Dorg.gradle.parallel=true"

# 時間計測付きでテスト実行
./scripts/measure-test-time.sh all
```

## 📝 次のステップ（教材用改善案）

1. **テストコンテナの共有化**
   - 現状: テストクラスごとにOracle起動
   - 改善: `withReuse(true)` で全テスト共有
   - **効果**: IT時間70-80%削減（3-6分 → 1分以内）

2. **パラレル実行**
   - `./gradlew test --parallel` で高速化

3. **カスタムアサーション**
   - AssertJのカスタムアサーションで可読性向上

## 📄 ライセンス

MIT License
